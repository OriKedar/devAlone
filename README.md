Welcome to task #1!

to run the server you need to start your local VM

run ```npm install``` to install all the dependencies

then, run ```npm run build``` to build the project. 

the after that you can run it with ```npm run serv``` and send your requests.